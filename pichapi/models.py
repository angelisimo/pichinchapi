from django.db import models

# Create your models here.

class Api(models.Model):
    Amessage = models.CharField(max_length=100, blank=True, default='')
    Ato = models.CharField(max_length 
    Afrom
    Atimetolifesec

